# Example terminal commands
mkdir -p .github/workflows
mv workflow-checker.yml .github/workflows/
git add .github/workflows/workflow-checker.yml
git commit -m "Add automatic workflow syntax checker"
git push origin main
